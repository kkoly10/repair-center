import { createClient } from '@supabase/supabase-js'

export function createBrowserClient(url, anonKey, options = {}) {
  return createClient(url, anonKey, options)
}

export function createServerClient(url, anonKey, options = {}) {
  return createClient(url, anonKey, options)
}
